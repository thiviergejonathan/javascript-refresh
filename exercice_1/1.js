/*
	Écrivez une fonction qui prend en entrée une température en degrés Celsius et la convertit en degrés Fahrenheit.

	°F = (°C × 9/5) + 32
*/

const celciusToFarenheit = celcius => { return ((celciux  * 9/5) + 32)}